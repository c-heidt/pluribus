"""Build-time machinery for the card-information abstraction.

This subpackage is only imported by the CLI entry point
(``poker_ai build-abstraction``) and its tests — never by the
runtime environment or training consumers.
"""
